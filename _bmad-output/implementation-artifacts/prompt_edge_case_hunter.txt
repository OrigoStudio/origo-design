Invoke the `bmad-review-edge-case-hunter` skill on this diff:

﻿diff --git a/_bmad-output/implementation-artifacts/sprint-status.yaml b/_bmad-output/implementation-artifacts/sprint-status.yaml
index ff1bc25..5ac9c0d 100644
--- a/_bmad-output/implementation-artifacts/sprint-status.yaml
+++ b/_bmad-output/implementation-artifacts/sprint-status.yaml
@@ -41,7 +41,7 @@
 # - Retrospective appends its action items to action_items; sprint-status surfaces open ones
 
 generated: 2026-07-29T21:46:02.464968
-last_updated: 2026-08-12T21:29:53+05:30
+last_updated: 2026-08-12T21:55:00+05:30
 project: origo-design
 project_key: NOKEY
 tracking_system: file-system
@@ -72,7 +72,7 @@ development_status:
   epic-3-retrospective: done
   3-5-1-establish-semantic-versioning: done
   3-5-2-define-monorepo-wide-json-import-standard: done
-  3-5-3-defensive-ast-traversal: ready-for-dev
+  3-5-3-defensive-ast-traversal: review
   epic-4: backlog
   4-1-capabilities-schema-parsing: backlog
   4-2-contracts-and-implementations: backlog
diff --git a/_bmad-output/implementation-artifacts/stories/3-5-3-defensive-ast-traversal.md b/_bmad-output/implementation-artifacts/stories/3-5-3-defensive-ast-traversal.md
index 4c2b64c..d20ef63 100644
--- a/_bmad-output/implementation-artifacts/stories/3-5-3-defensive-ast-traversal.md
+++ b/_bmad-output/implementation-artifacts/stories/3-5-3-defensive-ast-traversal.md
@@ -2,7 +2,8 @@
 epic: 3
 story: "5-3"
 title: Defensive AST Traversal
-status: ready-for-dev
+status: review
+baseline_commit: 4f82702e479e9639062c91d8097eb38a89c26fa8
 ---
 
 # Story 3.5.3: Defensive AST Traversal
@@ -80,6 +81,38 @@ This is the final tech debt and stabilization chore for Epic 3. The AST logic cu
 ### Project Context Reference
 - Epic 3's core deliverable is the Canonical AST validation engine. This stability fix is required before Epic 4 begins heavily extending the schemas with Capabilities, Contracts, and Extensions, which will add further tree complexity.
 
+## Tasks/Subtasks
+- [x] 1. Refactor `validateAST` and `dfs` in `ast-validator.ts` to enforce `MAX_AST_DEPTH` (e.g. 100) and immediately return circular reference validation errors instead of throwing Node.js Errors.
+- [x] 2. Refactor `canonicalize` in `serializer.ts` to accept depth limit and return errors gracefully rather than throwing.
+- [x] 3. Update `serializeAST` and other consumers to handle returned errors appropriately.
+- [x] 4. Add regression test fixtures for deep AST structure, circular entity references, and missing dependencies. Ensure benchmark tests remain within limits.
+
+## Dev Notes
+- Ensure the array of `ValidationError` includes specific type names (e.g. `MAX_DEPTH_EXCEEDED`, `CIRCULAR_REFERENCE`).
+- Remember dynamically generated deep AST payloads in tests to avoid JSON bloat.
+
+## Dev Agent Record
+### Debug Log
+- Fixed minor TypeScript strictness error in the deep AST generation fixture.
+
+### Completion Notes
+- Implemented `ValidationError` and `ASTValidationError` classes in `packages/core/src/types/validation.ts`.
+- Refactored `validateAST` to accumulate errors.
+- Enforced depth limit `MAX_AST_DEPTH = 250` in DFS graph traversal and recursive canonicalization.
+- Replaced untyped exceptions with structured validation errors, preventing Node crash edge cases.
+
+## File List
+- packages/core/src/types/validation.ts
+- packages/core/src/validator/ast-validator.ts
+- packages/core/src/validator/serializer.ts
+- packages/core/src/validator/ast-validator.spec.ts
+- packages/core/src/validator/serializer.spec.ts
+
+## Change Log
+- Added `MAX_AST_DEPTH` check.
+- Refactored cycle detection to return errors gracefully.
+- Updated spec tests for deep AST arrays and modified assertion errors.
+
 ## Completion Status
-Status: ready-for-dev
-Completion Note: Ultimate context engine analysis completed - comprehensive developer guide created.
+Status: review
+Completion Note: Defensive AST Traversal implementation completed with all acceptance criteria met. Tests are green.
diff --git a/packages/core/src/types/validation.ts b/packages/core/src/types/validation.ts
new file mode 100644
index 0000000..d3287ad
--- /dev/null
+++ b/packages/core/src/types/validation.ts
@@ -0,0 +1,18 @@
+export interface ValidationError {
+  type: string;
+  message: string;
+  path?: string;
+}
+
+export class ASTValidationError extends Error {
+  public errors: ValidationError[];
+
+  constructor(errors: ValidationError[], message: string = 'AST Validation Failed') {
+    super(message);
+    this.name = 'ASTValidationError';
+    this.errors = errors;
+
+    // Fix prototype chain for instanceof when extending built-in Error in TS
+    Object.setPrototypeOf(this, ASTValidationError.prototype);
+  }
+}
diff --git a/packages/core/src/validator/ast-validator.spec.ts b/packages/core/src/validator/ast-validator.spec.ts
index e6e5b8c..bdfb533 100644
--- a/packages/core/src/validator/ast-validator.spec.ts
+++ b/packages/core/src/validator/ast-validator.spec.ts
@@ -1,5 +1,5 @@
 import { CanonicalAST } from '../types/ast';
-import { validateAST } from './ast-validator';
+import { validateAST, MAX_AST_DEPTH } from './ast-validator';
 
 describe('AST Validation Engine', () => {
   it('should pass a valid Canonical AST', () => {
@@ -30,7 +30,7 @@ describe('AST Validation Engine', () => {
         },
       ],
     };
-    expect(() => validateAST(ast)).not.toThrow();
+    expect(validateAST(ast)).toEqual([]);
   });
 
   it('should detect a circular dependency between two entities', () => {
@@ -77,7 +77,10 @@ describe('AST Validation Engine', () => {
         },
       ],
     };
-    expect(() => validateAST(ast)).toThrow(
+    const errors = validateAST(ast);
+    expect(errors).toHaveLength(1);
+    expect(errors[0].type).toBe('CIRCULAR_REFERENCE');
+    expect(errors[0].message).toMatch(
       /Circular dependency detected: entity-1 -> entity-2 -> entity-1/
     );
   });
@@ -111,10 +114,13 @@ describe('AST Validation Engine', () => {
         },
       ],
     };
-    expect(() => validateAST(ast)).toThrow(/Circular dependency detected: entity-1 -> entity-1/);
+    const errors = validateAST(ast);
+    expect(errors).toHaveLength(1);
+    expect(errors[0].type).toBe('CIRCULAR_REFERENCE');
+    expect(errors[0].message).toMatch(/Circular dependency detected: entity-1 -> entity-1/);
   });
 
-  it('should throw an error for missing referenced entities', () => {
+  it('should report an error for missing referenced entities', () => {
     const ast: CanonicalAST = {
       schemaVersion: '1.0.0',
       domains: [
@@ -143,12 +149,15 @@ describe('AST Validation Engine', () => {
         },
       ],
     };
-    expect(() => validateAST(ast)).toThrow(
+    const errors = validateAST(ast);
+    expect(errors).toHaveLength(1);
+    expect(errors[0].type).toBe('MISSING_REFERENCE');
+    expect(errors[0].message).toMatch(
       /Invalid consumption rule: Entity "entity-missing" referenced by field "field-1" does not exist/
     );
   });
 
-  it('should throw an error if multiple domains declare the same entity ID (if unique entity IDs are required globally)', () => {
+  it('should report an error if multiple domains declare the same entity ID', () => {
     const ast: CanonicalAST = {
       schemaVersion: '1.0.0',
       domains: [
@@ -180,7 +189,10 @@ describe('AST Validation Engine', () => {
         },
       ],
     };
-    expect(() => validateAST(ast)).toThrow(/Duplicate entity ID found: entity-1/);
+    const errors = validateAST(ast);
+    expect(errors).toHaveLength(1);
+    expect(errors[0].type).toBe('DUPLICATE_ID');
+    expect(errors[0].message).toMatch(/Duplicate entity ID found: entity-1/);
   });
 
   it('should detect a three-node cycle', () => {
@@ -242,7 +254,10 @@ describe('AST Validation Engine', () => {
         },
       ],
     };
-    expect(() => validateAST(ast)).toThrow(
+    const errors = validateAST(ast);
+    expect(errors).toHaveLength(1);
+    expect(errors[0].type).toBe('CIRCULAR_REFERENCE');
+    expect(errors[0].message).toMatch(
       /Circular dependency detected: A -> B -> C -> A|B -> C -> A -> B|C -> A -> B -> C/
     );
   });
@@ -266,7 +281,7 @@ describe('AST Validation Engine', () => {
         },
       ],
     };
-    expect(() => validateAST(ast)).not.toThrow();
+    expect(validateAST(ast)).toEqual([]);
   });
 
   it('should pass a valid diamond dependency graph without false positive cycles', () => {
@@ -342,6 +357,50 @@ describe('AST Validation Engine', () => {
         },
       ],
     };
-    expect(() => validateAST(ast)).not.toThrow();
+    expect(validateAST(ast)).toEqual([]);
+  });
+
+  it('should report an error when MAX_AST_DEPTH is exceeded', () => {
+    const entities = [];
+    for (let i = 0; i <= MAX_AST_DEPTH + 1; i++) {
+      entities.push({
+        id: `e${i}`,
+        name: `e${i}`,
+        fields: [
+          {
+            id: `f${i}`,
+            name: `f${i}`,
+            type: 'string' as const,
+            references: `e${i + 1}`,
+            label: `f${i}`,
+            validation: [],
+            metadata_path: '',
+          },
+        ],
+      });
+    }
+    // Add the last entity that has no references
+    entities.push({
+      id: `e${MAX_AST_DEPTH + 2}`,
+      name: `e${MAX_AST_DEPTH + 2}`,
+      fields: [],
+    });
+
+    const ast: CanonicalAST = {
+      schemaVersion: '1.0.0',
+      domains: [
+        {
+          id: 'domain-1',
+          name: 'Core',
+          version: '1.0.0',
+          domain: 'core',
+          entities: entities,
+        },
+      ],
+    };
+
+    const errors = validateAST(ast);
+    expect(errors.length).toBeGreaterThan(0);
+    expect(errors.some(e => e.type === 'MAX_DEPTH_EXCEEDED')).toBe(true);
   });
 });
diff --git a/packages/core/src/validator/ast-validator.ts b/packages/core/src/validator/ast-validator.ts
index da31847..b20f767 100644
--- a/packages/core/src/validator/ast-validator.ts
+++ b/packages/core/src/validator/ast-validator.ts
@@ -1,15 +1,21 @@
 import { CanonicalAST } from '../types/ast';
+import { ValidationError } from '../types/validation';
+
+export const MAX_AST_DEPTH = 250;
 
 /**
  * Performs deep semantic validation on the Canonical AST before downstream code generation.
  * Specifically checks for cyclic entity relationships and missing references.
  *
  * @param ast The Canonical AST to validate
- * @throws {Error} If validation fails
+ * @returns {ValidationError[]} Array of validation errors, or empty array if valid.
  */
-export function validateAST(ast: CanonicalAST): void {
+export function validateAST(ast: CanonicalAST): ValidationError[] {
+  const errors: ValidationError[] = [];
+
   if (!ast || !Array.isArray(ast.domains)) {
-    throw new Error('Invalid AST: missing domains array');
+    errors.push({ type: 'INVALID_FORMAT', message: 'Invalid AST: missing domains array' });
+    return errors;
   }
   const entityMap = new Map<string, string>();
 
@@ -18,9 +24,14 @@ export function validateAST(ast: CanonicalAST): void {
     const entities = Array.isArray(domain.entities) ? domain.entities : [];
     for (const entity of entities) {
       if (entityMap.has(entity.id)) {
-        throw new Error(`Duplicate entity ID found: ${entity.id}`);
+        errors.push({
+          type: 'DUPLICATE_ID',
+          message: `Duplicate entity ID found: ${entity.id}`,
+          path: entity.id,
+        });
+      } else {
+        entityMap.set(entity.id, entity.name);
       }
-      entityMap.set(entity.id, entity.name);
     }
   }
 
@@ -35,11 +46,14 @@ export function validateAST(ast: CanonicalAST): void {
       for (const field of fields) {
         if (field.references) {
           if (!entityMap.has(field.references)) {
-            throw new Error(
-              `Invalid consumption rule: Entity "${field.references}" referenced by field "${field.id}" does not exist`
-            );
+            errors.push({
+              type: 'MISSING_REFERENCE',
+              message: `Invalid consumption rule: Entity "${field.references}" referenced by field "${field.id}" does not exist`,
+              path: field.id,
+            });
+          } else {
+            dependencies.push(field.references);
           }
-          dependencies.push(field.references);
         }
       }
       adjList.set(entity.id, dependencies);
@@ -50,13 +64,27 @@ export function validateAST(ast: CanonicalAST): void {
   const visited = new Set<string>();
   const visiting = new Set<string>();
 
-  function dfs(nodeId: string, path: string[]) {
+  function dfs(nodeId: string, path: string[], depth: number) {
+    if (depth > MAX_AST_DEPTH) {
+      errors.push({
+        type: 'MAX_DEPTH_EXCEEDED',
+        message: `Maximum AST depth exceeded (${MAX_AST_DEPTH}) at entity ${nodeId}`,
+        path: path.join(' -> '),
+      });
+      return;
+    }
+
     if (visiting.has(nodeId)) {
       // Cycle detected: isolate the loop part
       const cycleStartIndex = path.indexOf(nodeId);
       const loop = cycleStartIndex >= 0 ? path.slice(cycleStartIndex) : path;
       const cyclePath = [...loop, nodeId].join(' -> ');
-      throw new Error(`Circular dependency detected: ${cyclePath}`);
+      errors.push({
+        type: 'CIRCULAR_REFERENCE',
+        message: `Circular dependency detected: ${cyclePath}`,
+        path: cyclePath,
+      });
+      return;
     }
 
     if (visited.has(nodeId)) {
@@ -66,9 +94,9 @@ export function validateAST(ast: CanonicalAST): void {
     visiting.add(nodeId);
     path.push(nodeId);
 
-    const deps = adjList.get(nodeId)!;
+    const deps = adjList.get(nodeId) || [];
     for (const dep of deps) {
-      dfs(dep, path);
+      dfs(dep, path, depth + 1);
     }
 
     path.pop();
@@ -78,7 +106,9 @@ export function validateAST(ast: CanonicalAST): void {
 
   for (const entityId of entityMap.keys()) {
     if (!visited.has(entityId)) {
-      dfs(entityId, []);
+      dfs(entityId, [], 1);
     }
   }
+
+  return errors;
 }
diff --git a/packages/core/src/validator/serializer.spec.ts b/packages/core/src/validator/serializer.spec.ts
index 82e4265..3eca08a 100644
--- a/packages/core/src/validator/serializer.spec.ts
+++ b/packages/core/src/validator/serializer.spec.ts
@@ -1,5 +1,7 @@
 import { CanonicalAST } from '../types/ast';
 import { serializeAST } from './serializer';
+import { ASTValidationError } from '../types/validation';
+import { MAX_AST_DEPTH } from './ast-validator';
 
 describe('AST Serializer', () => {
   it('should embed schemaVersion in the root payload', () => {
@@ -153,14 +155,14 @@ describe('AST Serializer', () => {
     expect(parsed.domains[0].capabilities[1].name).toBe('Cap B');
   });
 
-  it('should throw if schemaVersion is missing', () => {
+  it('should throw ASTValidationError if schemaVersion is missing', () => {
     const ast: any = { domains: [] };
-    expect(() => serializeAST(ast)).toThrow('Invalid AST: Missing schemaVersion');
+    expect(() => serializeAST(ast)).toThrow(ASTValidationError);
   });
 
-  it('should throw if domains is missing or not an array', () => {
+  it('should throw ASTValidationError if domains is missing or not an array', () => {
     const ast: any = { schemaVersion: '1.0.0' };
-    expect(() => serializeAST(ast)).toThrow('Invalid AST: domains must be an array');
+    expect(() => serializeAST(ast)).toThrow(ASTValidationError);
   });
 
   it('should guarantee array sorting for primitive arrays', () => {
@@ -188,14 +190,24 @@ describe('AST Serializer', () => {
     expect(parsed.items[1].id).toBe('2');
   });
 
-  it('should detect and throw on circular references', () => {
+  it('should detect and throw ASTValidationError on circular references during canonicalization', () => {
     const ast: any = { schemaVersion: '1.0.0', domains: [] };
     ast.self = ast;
-    expect(() => serializeAST(ast)).toThrow('Invalid AST: Circular reference detected');
+    expect(() => serializeAST(ast)).toThrow(ASTValidationError);
   });
 
-  it('should throw on unsupported types like Date', () => {
+  it('should throw ASTValidationError on unsupported types like Date', () => {
     const ast: any = { schemaVersion: '1.0.0', domains: [], date: new Date() };
-    expect(() => serializeAST(ast)).toThrow('Invalid AST: Unsupported type Date');
+    expect(() => serializeAST(ast)).toThrow(ASTValidationError);
+  });
+
+  it('should throw ASTValidationError on exceeding MAX_AST_DEPTH during canonicalization', () => {
+    let deepAst: any = { schemaVersion: '1.0.0', domains: [] };
+    let current = deepAst;
+    for (let i = 0; i <= MAX_AST_DEPTH + 1; i++) {
+      current.child = {};
+      current = current.child;
+    }
+    expect(() => serializeAST(deepAst)).toThrow(ASTValidationError);
   });
 });
diff --git a/packages/core/src/validator/serializer.ts b/packages/core/src/validator/serializer.ts
index 0f98775..bcf971d 100644
--- a/packages/core/src/validator/serializer.ts
+++ b/packages/core/src/validator/serializer.ts
@@ -1,28 +1,45 @@
 import { CanonicalAST } from '../types/ast';
-import { validateAST } from './ast-validator';
+import { validateAST, MAX_AST_DEPTH } from './ast-validator';
+import { ASTValidationError } from '../types/validation';
 
 /**
  * Deep clones and canonically sorts an object/array.
  * Also detects circular references and unsupported types.
  */
-function canonicalize(obj: any, seen = new WeakSet()): any {
+function canonicalize(obj: any, seen = new WeakSet(), depth = 1): any {
+  if (depth > MAX_AST_DEPTH) {
+    throw new ASTValidationError([
+      {
+        type: 'MAX_DEPTH_EXCEEDED',
+        message: `Maximum AST depth exceeded (${MAX_AST_DEPTH}) during canonicalization`,
+      },
+    ]);
+  }
+
   if (obj === null || typeof obj !== 'object') {
     return obj;
   }
 
   if (seen.has(obj)) {
-    throw new Error('Invalid AST: Circular reference detected');
+    throw new ASTValidationError([
+      {
+        type: 'CIRCULAR_REFERENCE',
+        message: 'Invalid AST: Circular reference detected during canonicalization',
+      },
+    ]);
   }
 
   if (obj.constructor && obj.constructor !== Object && !Array.isArray(obj)) {
-    throw new Error(`Invalid AST: Unsupported type ${obj.constructor.name}`);
+    throw new ASTValidationError([
+      { type: 'INVALID_TYPE', message: `Invalid AST: Unsupported type ${obj.constructor.name}` },
+    ]);
   }
 
   seen.add(obj);
 
   if (Array.isArray(obj)) {
     // Process children first
-    const mapped = obj.map(item => canonicalize(item, seen));
+    const mapped = obj.map(item => canonicalize(item, seen, depth + 1));
 
     // Sort deterministically
     mapped.sort((a, b) => {
@@ -56,7 +73,7 @@ function canonicalize(obj: any, seen = new WeakSet()): any {
   const sortedKeys = Object.keys(obj).sort();
   const result: Record<string, any> = {};
   for (const key of sortedKeys) {
-    result[key] = canonicalize(obj[key], seen);
+    result[key] = canonicalize(obj[key], seen, depth + 1);
   }
 
   seen.delete(obj);
@@ -69,18 +86,26 @@ function canonicalize(obj: any, seen = new WeakSet()): any {
  *
  * @param ast The canonical AST to serialize
  * @returns Byte-for-byte deterministic JSON string
+ * @throws {ASTValidationError} If validation or canonicalization fails
  */
 export function serializeAST(ast: CanonicalAST): string {
   if (!ast || !ast.schemaVersion) {
-    throw new Error('Invalid AST: Missing schemaVersion');
+    throw new ASTValidationError([
+      { type: 'INVALID_FORMAT', message: 'Invalid AST: Missing schemaVersion' },
+    ]);
   }
 
   if (!Array.isArray(ast.domains)) {
-    throw new Error('Invalid AST: domains must be an array');
+    throw new ASTValidationError([
+      { type: 'INVALID_FORMAT', message: 'Invalid AST: domains must be an array' },
+    ]);
   }
 
   // Validate semantics before serializing
-  validateAST(ast);
+  const errors = validateAST(ast);
+  if (errors.length > 0) {
+    throw new ASTValidationError(errors, 'AST Validation Failed');
+  }
 
   const canonical = canonicalize(ast);
 

